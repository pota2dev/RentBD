import PropertySearchView from './view';

export const metadata = {
  title: 'Property Search | RentBD',
  description: 'Search and filter properties with advanced sorting options'
};

export default function PropertySearchPage() {
  return <PropertySearchView />;
}
